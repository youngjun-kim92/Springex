<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd http://www.springframework.org/schema/context
       https://www.springframework.org/schema/context/spring-context.xsd">

    <!-- 스프링 mvc 설정을 에너테이션 기반으로 처리한다는 의미.
         스프링 mvc의 여러 객체들을 자동으로 스프링 빈(Bean)으로 등록하게 하는 기능이다. -->
    <mvc:annotation-driven></mvc:annotation-driven>

    <!-- 정적경로 파일 경로를 지정, html, css, images 등 -->
    <!-- webapp에 resources가 없기 때문에 만들어준다 -->
    <mvc:resources mapping="/resources/**" location="/resources/"></mvc:resources>

    <!-- 스프링 mvc에서 제공하는 뷰(view)를 어떻게 결정했는지에 대한 설정 -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/"></property>
        <property name="suffix" value=".jsp"></property>
    </bean>

    <context:component-scan base-package="com.springex.controller"/>

    <!-- "conversionService라는 빈(bean)을 등록한 후에 스프링 mvc를 처리할 때 이용하겠다" 라는 의미 -->
    <bean id="conversionService" class="org.springframework.format.support.FormattingConversionServiceFactoryBean">
        <property name="formatters">
            <set>
                <bean class="com.springex.controller.formatter.LocalDateFormatter"/>
            </set>
        </property>
    </bean>

    <mvc:annotation-driven  conversion-service="conversionService" />
</beans>